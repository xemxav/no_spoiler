## Agent skills

### Issue tracker

Issues and specs live as GitHub issues (repo isn't connected to a remote yet — confirm one exists before `/to-tickets` or `/triage` run). See `docs/agents/issue-tracker.md`.

### Triage labels

Default five canonical roles, label strings equal to their names. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context — one `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
