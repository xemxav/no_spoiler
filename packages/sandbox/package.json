{
  "name": "@no-spoiler/sandbox",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "sandbox": "./dist/cli.js"
  },
  "scripts": {
    "dev": "tsc --watch --preserveWatchOutput",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "typescript": "^5.9.3",
    "vite": "^8.3.0",
    "vitest": "^5.0.1"
  }
}
