{
  "name": "@no-spoiler/extension",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "mkdir -p dist && cp manifest.json popup.html dist/ && tsc --watch --preserveWatchOutput",
    "build": "tsc -p tsconfig.json && cp manifest.json popup.html dist/",
    "test": "vitest run",
    "lint": "eslint ."
  },
  "dependencies": {
    "@no-spoiler/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/chrome": "^0.3.0",
    "jsdom": "^30.1.0",
    "typescript": "^5.9.3",
    "vite": "^8.3.0",
    "vitest": "^5.0.1"
  }
}
