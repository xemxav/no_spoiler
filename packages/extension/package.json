{
  "name": "@no-spoiler/extension",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "mkdir -p dist && cp manifest.json popup.html dist/ && (tsc --watch --preserveWatchOutput & esbuild src/content-script.ts src/background.ts --bundle --outdir=dist --format=iife --platform=browser --define:__BACKEND_URL__=\"\\\"${BACKEND_URL:-http://localhost:3210}\\\"\" --define:__BACKEND_AUTH_TOKEN__=\"\\\"${BACKEND_AUTH_TOKEN:-}\\\"\" --watch)",
    "build": "tsc -p tsconfig.json && esbuild src/content-script.ts src/background.ts --bundle --outdir=dist --format=iife --platform=browser --define:__BACKEND_URL__=\"\\\"${BACKEND_URL:-http://localhost:3210}\\\"\" --define:__BACKEND_AUTH_TOKEN__=\"\\\"${BACKEND_AUTH_TOKEN:-}\\\"\" && cp manifest.json popup.html dist/",
    "test": "vitest run",
    "lint": "eslint ."
  },
  "dependencies": {
    "@no-spoiler/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/chrome": "^0.3.0",
    "esbuild": "^0.28.2",
    "jsdom": "^30.1.0",
    "typescript": "^5.9.3",
    "vite": "^8.3.0",
    "vitest": "^5.0.1"
  }
}
